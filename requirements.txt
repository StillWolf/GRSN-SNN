# 实测可跑通的版本组合（详见 docs/SETUP.md）
# Python 3.10 / PyTorch 2.4 / Linux

# Core
torch>=2.0,<2.5      # 与 spikingjelly clock_driven API 兼容
numpy<2              # 老版 gym 不兼容 numpy 2.x
scipy>=1.10

# RL environment
gym==0.26.2          # SMAC / 老 pomdp wrapper 都依赖此版本
pybullet>=3.2.5      # PyBullet POMDP 环境
pycolab              # KeyToDoor 信用分配环境

# Configuration
ruamel.yaml>=0.16

# SNN
spikingjelly>=0.0.0.0.14

# Utilities / logging
tqdm
pandas
matplotlib
seaborn
tensorboard          # 可选

# Testing
pytest>=6
